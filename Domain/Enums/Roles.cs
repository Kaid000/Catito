namespace Domain.Enums
{
    /// <summary>
    /// Represents the roles that a user can have.
    /// </summary>
    public enum Roles
    {
        /// <summary>
        /// Administrator with full access.
        /// </summary>
        Admin,
    }
}
